{
    "properties": [
        {
            "name": "encryption.secret",
            "type": "java.lang.String",
            "description": "The secret key used to encrypt and decrypt the data (base64 encoded)."
        }
    ]
}